apiVersion: apps/v1
kind: Deployment
metadata:
  name: cst-omie-sorocaba-frontend
  namespace: cst-omie-sorocaba
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cst-omie-sorocaba-frontend
  template:
    metadata:
      labels:
        app: cst-omie-sorocaba-frontend
    spec:
      containers:
        - name: cst-omie-sorocaba-frontend-container
          image: ghcr.io/oondemand/cst-omie-sorocaba-frontend:${IMAGE_TAG}-prod
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "64Mi"
              cpu: "0.05"
            limits:
              memory: "128Mi"
              cpu: "0.1"
