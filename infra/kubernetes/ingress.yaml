apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cst-omie-sorocaba-frontend-ingress
  namespace: cst-omie-sorocaba
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-nginx
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - cst-omie-sorocaba.oondemand.online
      secretName: app-tls
  rules:
    - host: cst-omie-sorocaba.oondemand.online
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cst-omie-sorocaba-frontend-service
                port:
                  number: 80
