apiVersion: v1
kind: Service
metadata:
  name: cst-omie-sorocaba-frontend-homolog-service
  namespace: cst-omie-sorocaba-homolog
spec:
  type: NodePort
  selector:
    app: cst-omie-sorocaba-frontend-homolog
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
